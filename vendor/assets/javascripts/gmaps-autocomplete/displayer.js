// Generated by LiveScript 1.2.0
(function(){
  var Displayer;
  module.exports = Displayer = (function(){
    Displayer.displayName = 'Displayer';
    var prototype = Displayer.prototype, constructor = Displayer;
    function Displayer(completer){
      this.completer = completer;
      this.map = this.completer.map;
      this.element = this.completer.config.element;
      this.marker = this.completer.marker;
    }
    prototype.updateMap = function(geometry){
      if (this.map) {
        this.map.fitBounds(geometry.viewport);
      }
      if (this.marker) {
        return this.marker.setPosition(geometry.location);
      }
    };
    prototype.updateUI = function(address, position, geocoderResponse){
      var updateAdr;
      $(this.element.input).autocomplete('close');
      this.debug('country', this.country);
      this.debug('geocoderResponse', geocoderResponse);
      address.replace(', ' + country, '');
      updateAdr = address;
      this.debug('update address', updateAdr);
      $(this.element.input).val(updateAdr);
      return this.positionOutputter(position);
    };
    prototype.positionOutputter = function(position){
      $(this.element.latitude).html(position.lat());
      return $(this.element.longitude).html(position.lng());
    };
    return Displayer;
  }());
}).call(this);
